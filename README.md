# LZSS - WORK IN PROGRESS
LZSS Compression, non-standard implementation.

The compression side of things looks to be working, binary style B)
The next step is to implement the decompression algorith, which should show us if there's any more errors in the compression side of things c:

To compile this, run `make`
to compile this with debug output enabled, run `make debug` 
